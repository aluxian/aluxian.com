# aluxian.com

Made w/ Gulp, Jade, SASS & CoffeeScript.

Install dependencies:

    npm install -g gulp
    npm install
    bower install

Production build:

    gulp live build

Dev mode:

    gulp watch
