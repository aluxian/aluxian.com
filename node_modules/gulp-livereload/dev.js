'use strict';

// var util = require('util'),
//     spawn = require('child_process').spawn;


// require('sane')(__dirname, ['gulp-livereload.js'])
//   .on('change', function() {
//     var cmd;

//     util.log('changed detected');

//     cmd = spawn('./node_modules/.bin/mocha', ['--bail', '--reporter=spec']);

//     cmd.stdout.pipe(process.stdout);
//     cmd.stderr.pipe(process.stderr);

//     cmd.on('close', function() {
//       util.log('test done');
//     });
//   });

var m = require('lodash.merge');

m(exports.a, {name: 'c'});
console.log(exports.a);
