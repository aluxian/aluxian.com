/*jslint browser:true */
(function () {
    "use strict";

    // Declare Vars
    var hello       = 'Hello',
        world       = 'World';

    // Functions
    function helloWorld() {
        console.log(hello + ', ' + world + '!');
    }

    // Call Functions
    helloWorld();

}());